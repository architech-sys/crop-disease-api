services:
  - type: web
    name: crop-disease-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn crop_disease_api:app --host 0.0.0.0 --port 10000
    plan: free